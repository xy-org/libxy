import xytest;
import libxy.(time, unit);
import ..;

func func1() {
    #env := receive();
    #env.sender'send(Msg{env.msg.id + 1});
}

*func spawing~Test() {
    sid := spawn($func1()'toCallback);
    sid'send(Msg{5});
    # receive();
}
