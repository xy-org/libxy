import libxy.process;
import libxy.string;
import libxy.list;
import libxy.error;
import libxy.memory;
import xytest;

*func runningLs~Test() {
    args : List~Str~MemStatic;
    args'append("Echoing from test");
    p := run("echo", args);
    _ = p'wait();
}
