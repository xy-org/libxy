import xytest;
import libxy.(string, list, error, memory);
import ..;

*func runningLs~Test() {
    args : List~String;
    args'append("Echoing from test");
    p := run("echo", args);
    _ = p'wait();
}
