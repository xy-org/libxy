import libxy.process;
import libxy.string;
import libxy.array;
import libxy.error;
import libxy.memory;
import xytest;

*def runningLs~Test() {
    args : Array~Str~MemStatic;
    args'append("Echoing from test");
    p := run("echo", args);
    _ = p'wait();
}
