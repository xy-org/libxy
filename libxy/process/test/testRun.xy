import xytest;
import libxy.(string, list, error, memory);
import ..;

*func runningLs~Test() {
    args : List~Str~MemStatic;
    args'append("Echoing from test");
    p := run("echo", args);
    _ = p'wait();
}
